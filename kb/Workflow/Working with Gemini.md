# Working with Gemini

- Start with `GEMINI.md`, then follow `AGENTS.md`.
- Use targeted search and only open the pages needed for the task.
- Keep project knowledge in the vault instead of duplicating it in prompts.
